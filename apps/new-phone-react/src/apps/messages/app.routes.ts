import { AppRoute } from '@navigation/navigator';
import { Messages } from './screens/Messages';

export default {
    name: 'Messages',
    path: '/messages',
    Component: Messages,
} satisfies AppRoute;
