import { ThemeOptions } from '@mui/material';

const theme: ThemeOptions = {
  palette: {
    primary: {
      main: '#202128',
    },
    secondary: {
      main: '#2596be',
    },
  },
};

export default theme;
