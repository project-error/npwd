import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles((theme) => ({
  closeButton: {
    width: '10%',
    position: 'absolute',
    right: 0
  },
  input: {
    
  },
  inputStyles: {
     
  }
}))

export default useStyles;