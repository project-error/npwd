import { createAppThemeProvider } from '../../../os/apps/utils/createAppThemeProvider';

import selloutTheme from '../sellout.theme';

export const SelloutThemeProvider = createAppThemeProvider(selloutTheme);
